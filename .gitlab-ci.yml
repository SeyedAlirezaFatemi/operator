image: node:22

pages:
  stage: deploy
  cache:
    paths:
      - node_modules/
  script:
    - npm ci
    - npm run build
  pages:
    publish: build/client
  artifacts:
    paths:
      - build/client
  rules:
    - if: '$CI_COMMIT_BRANCH == "master"'
